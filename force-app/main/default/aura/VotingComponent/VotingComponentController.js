({ 
    increment: function(component, event, helper){ 
        var count = parseInt(component.get("v.num1")+1);
        component.set("v.num1", count);    
    },
    decrement: function(component, event, helper){ 
        var count = parseInt(component.get("v.num1")-1);
        component.set("v.num1", count);
    },
    increment2: function(component, event, helper){ 
        var count = parseInt(component.get("v.num2")+1);
        component.set("v.num2", count);
    },
    decrement2: function(component, event, helper){ 
        var count = parseInt(component.get("v.num2")-1);
        component.set("v.num2", count);
    },
    increment3: function(component, event, helper){ 
        var count = parseInt(component.get("v.num3")+1);
        component.set("v.num3", count);    
    },
    decrement3: function(component, event, helper){ 
        var count = parseInt(component.get("v.num3")-1);
        component.set("v.num3", count);
    },
     increment4: function(component, event, helper){ 
        var count = parseInt(component.get("v.num4")+1);
        component.set("v.num4", count);
    },
    decrement4: function(component, event, helper){ 
        var count = parseInt(component.get("v.num4")-1);
        component.set("v.num4", count);
    },
         increment5: function(component, event, helper){ 
        var count = parseInt(component.get("v.num5")+1);
        component.set("v.num5", count);
    },
    decrement5: function(component, event, helper){ 
        var count = parseInt(component.get("v.num5")-1);
        component.set("v.num5", count);
    },
         increment6: function(component, event, helper){ 
        var count = parseInt(component.get("v.num6")+1);
        component.set("v.num6", count);
    },
    decrement6: function(component, event, helper){ 
        var count = parseInt(component.get("v.num6")-1);
        component.set("v.num6", count);
    },
         increment7: function(component, event, helper){ 
        var count = parseInt(component.get("v.num7")+1);
        component.set("v.num7", count);
    },
    decrement7: function(component, event, helper){ 
        var count = parseInt(component.get("v.num7")-1);
        component.set("v.num7", count);
    },
         increment8: function(component, event, helper){ 
        var count= parseInt(component.get("v.num8")+1);
        component.set("v.num8", count);
    },
    decrement8: function(component, event, helper){ 
        var count = parseInt(component.get("v.num8")-1);
        component.set("v.num8", count);
    },
         increment9: function(component, event, helper){ 
        var count = parseInt(component.get("v.num9")+1);
        component.set("v.num9", count);
    },
    decrement9: function(component, event, helper){ 
        var count = parseInt(component.get("v.num9")-1);
        component.set("v.num9", count);
    },
         increment10: function(component, event, helper){ 
        var count = parseInt(component.get("v.num10")+1);
        component.set("v.num10", count);
    },
    decrement10: function(component, event, helper){ 
        var count = parseInt(component.get("v.num10")-1);
        component.set("v.num10", count);
    },
         increment11: function(component, event, helper){ 
        var count = parseInt(component.get("v.num11")+1);
        component.set("v.num11", count);
    },
    decrement11: function(component, event, helper){ 
        var count = parseInt(component.get("v.num11")-1);
        component.set("v.num11", count);
    },
         increment12: function(component, event, helper){ 
        var count = parseInt(component.get("v.num12")+1);
        component.set("v.num12", count);
    },
    decrement12: function(component, event, helper){ 
        var count = parseInt(component.get("v.num12")-1);
        component.set("v.num12", count);
    },
         increment13: function(component, event, helper){ 
        var count = parseInt(component.get("v.num13")+1);
        component.set("v.num13", count);
    },
    decrement13: function(component, event, helper){ 
        var count = parseInt(component.get("v.num13")-1);
        component.set("v.num13", count);
    },
         increment14: function(component, event, helper){ 
        var count = parseInt(component.get("v.num14")+1);
        component.set("v.num14", count);
    },
    decrement14: function(component, event, helper){ 
        var count = parseInt(component.get("v.num14")-1);
        component.set("v.num14", count);
    },
         increment15: function(component, event, helper){ 
        var count = parseInt(component.get("v.num15")+1);
        component.set("v.num15", count);
    },
    decrement15: function(component, event, helper){ 
        var count = parseInt(component.get("v.num15")-1);
        component.set("v.num15", count);
    },
         increment16: function(component, event, helper){ 
        var count = parseInt(component.get("v.num16")+1);
        component.set("v.num16", count);
    },
    decrement16: function(component, event, helper){ 
        var count = parseInt(component.get("v.num16")-1);
        component.set("v.num16", count);
    },
         increment17: function(component, event, helper){ 
        var count = parseInt(component.get("v.num17")+1);
        component.set("v.num17", count);
    },
    decrement17: function(component, event, helper){ 
        var count = parseInt(component.get("v.num17")-1);
        component.set("v.num17", count);
    },
         increment18: function(component, event, helper){ 
        var count= parseInt(component.get("v.num18")+1);
        component.set("v.num18", count);
    },
    decrement18: function(component, event, helper){ 
        var count = parseInt(component.get("v.num18")-1);
        component.set("v.num18", count);
    },
         increment19: function(component, event, helper){ 
        var count = parseInt(component.get("v.num19")+1);
        component.set("v.num19", count);
    },
    decrement19: function(component, event, helper){ 
        var count = parseInt(component.get("v.num19")-1);
        component.set("v.num19", count);
    },
         increment20: function(component, event, helper){ 
        var count= parseInt(component.get("v.num20")+1);
        component.set("v.num20", count);
    },
    decrement20: function(component, event, helper){ 
        var count = parseInt(component.get("v.num20")-1);
        component.set("v.num20", count);
    },
         increment21: function(component, event, helper){ 
        var count = parseInt(component.get("v.num21")+1);
        component.set("v.num21", count);
    },
    decrement22: function(component, event, helper){ 
        var count = parseInt(component.get("v.num22")-1);
        component.set("v.num22", count);
    },
         increment23: function(component, event, helper){ 
        var count = parseInt(component.get("v.num23")+1);
        component.set("v.num23", count);
    },
    decrement23: function(component, event, helper){ 
        var count = parseInt(component.get("v.num23")-1);
        component.set("v.num23", count);
    },
         increment24: function(component, event, helper){ 
        var count = parseInt(component.get("v.num24")+1);
        component.set("v.num24", count);
    },
    decrement24: function(component, event, helper){ 
        var count = parseInt(component.get("v.num24")-1);
        component.set("v.num24", count);
    },
})